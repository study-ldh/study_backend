spring:
  application:
    name: study

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://ec2-52-78-98-7.ap-northeast-2.compute.amazonaws.com:3306/study?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: study
    password: 1q2w3e4r

  jpa:
    hibernate:
      ddl-auto: update # 개발 초기에는 테이블 자동 생성을 위해 update 사용
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 8888